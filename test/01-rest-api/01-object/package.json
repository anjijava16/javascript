{
  "name": "application-name",
  "version": "0.0.1",
  "devDependencies": {
    "body-parser": "^1.12.2",
    "chai": "^2.1.2",
    "express": "^4.10.6",
    "loadtest": "^1.2.10",
    "require-dir": "^0.3.0",
    "supertest": "^0.15.0"
  },
  "scripts": {
    "pretest": "npm install",
    "test": "node_modules/.bin/mocha --recursive --reporter spec --timeout 30000 test",
    "test_stress": "node_modules/.bin/mocha --recursive --reporter spec --timeout 30000 test/stress"
  }
}
