{
  "name": "application-name",
  "version": "0.0.1",
  "devDependencies": {
    "mocha": "^2.0.1"
  },
  "scripts": {
    "pretest": "npm install",
    "test": "node_modules/.bin/mocha --recursive --timeout 30000"
  }
}
