{
  "name": "application-name",
  "version": "0.0.1",
  "devDependencies": {
    "chai": "^2.3.0",
    "express": "^4.12.2",
    "mocha": "^2.1.0",
    "supertest": "^0.15.0"
  },
  "scripts": {
    "pretest": "npm install",
    "test": "node_modules/.bin/mocha --recursive --reporter spec --timeout 30000 test"
  }
}
