{
  "name": "application-name",
  "version": "0.0.1",
  "devDependencies": {
    "body-parser": "^1.12.2",
    "express": "^4.12.2",
    "supertest": "^0.15.0",
    "chai": "^3.2.0",
    "deasync": "^0.1.0",
    "mocha": "^2.2.1",
    "parallel-mocha": "0.0.7"
  },
  "scripts": {
    "pretest": "npm install",
    "test": "node_modules/.bin/mocha --recursive --timeout 30000 test"
  }
}
