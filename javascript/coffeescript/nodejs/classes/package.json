{
  "name": "application-name",
  "version": "0.0.1",
  "devDependencies": {
    "coffee-script": "^1.8.0"
  },
  "scripts": {
    "compile": "node_modules/coffee-script/bin/coffee --bare --output server --compile coffeeServer",
    "test": "npm run compile && node server/test.js"
  }
}
