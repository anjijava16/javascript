{
  "name": "application-name",
  "version": "0.0.1",
  "devDependencies": {
    "nconf": "^0.7.1"
  },
    "scripts": {
        "test": "export NODE_ENV=test && node test/config-file-test.js"
    }
}
